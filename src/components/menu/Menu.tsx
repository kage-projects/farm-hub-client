import { Menu as ChakraMenu } from '@chakra-ui/react';

/**
 * Menu component - re-export from Chakra
 * - MenuButton trigger
 * - MenuList dropdown container
 * - MenuItem individual actions
 * - MenuSeparator for grouping
 * - Keyboard navigation support
 * - Hover/active states with brand colors
 */
export const Menu = ChakraMenu;

